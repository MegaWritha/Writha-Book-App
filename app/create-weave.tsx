import { View, Text } from "react-native";

export default function CreateWeave() {
  return (
    <View style={{ flex: 1, backgroundColor: "#000", justifyContent: "center", alignItems: "center" }}>
      <Text style={{ color: "#fff" }}>
        Weave Creation Screen (Next Stage)
      </Text>
    </View>
  );
}